{
  "name": "totalcap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install": "npm install --prefix frontend --legacy-peer-deps"
  }
}
