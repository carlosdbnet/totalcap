{
  "name": "totalcap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && cp -rv api frontend/dist/ && cp -rv backend frontend/dist/ && cp -rv database frontend/dist/ && cp -v requirements.txt frontend/dist/",
    "install": "npm install --prefix frontend --legacy-peer-deps"
  }
}
