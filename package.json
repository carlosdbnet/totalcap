{
  "name": "totalcap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && mkdir -p public && cp -rv frontend/dist/* public/",
    "install": "npm install --prefix frontend --legacy-peer-deps"
  }
}
