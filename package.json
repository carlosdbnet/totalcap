{
  "name": "totalcap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && mkdir -p production_build && cp -rv frontend/dist/* production_build/ && cp -rv api production_build/ && cp -rv backend production_build/ && cp -rv database production_build/ && cp -v requirements.txt production_build/",
    "install": "npm install --prefix frontend --legacy-peer-deps"
  }
}
